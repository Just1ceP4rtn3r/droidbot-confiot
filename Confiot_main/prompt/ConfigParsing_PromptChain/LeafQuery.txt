You are an expert mobile app analyst. Your task is to analyze the provided information of a single UI screen and determine its primary purposes or functions.

Based on the Page ID, the plain text, and the list of operations (like buttons and input fields), provide a list of app features summarized with a "one-sentence summary" for each, describing what a user can do on that page.


**Core Principle**
A single UI screen can have multiple, unrelated functions (e.g., a "Settings" page might have both 'Change Password' and 'Set Notification Preferences'). Do NOT group these into a single, generic function like 'Manage user settings'. You must identify each distinct function and create a separate JSON object for it in the output list.


**Output Format:**
```json
{
  "function": "str",
  "sequence": "list[operation]"
}
```

**Instructions**

function: Provide a concise "one-sentence summary" of the features of the app page.

sequence: List the specific Operations a user needs to perform on the current page to fulfill the function described above. Use only the predefined Operation types provided to you. The sequence should represent the most direct path to achieve the page's main function.

reason: Justify why the listed Operations are necessary and sufficient to accomplish the stated function on this particular page.



**Page Information Input**
